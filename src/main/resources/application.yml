server:
  port: 8080

# 默认激活 dev profile（适用于 JAR 直接运行）
# Maven 运行时通过 pom.xml 插件配置激活
spring:
  profiles:
    active: dev

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.dashu.dbtool.model.entity